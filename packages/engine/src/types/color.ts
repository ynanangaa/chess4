/**
 * The four player colors in a four-player chess game.
 *
 * Turn order convention (see {@link Game}) typically follows this list:
 * RED → BLUE → YELLOW → GREEN.
 */
export enum Color {
  RED = "red",
  BLUE = "blue",
  YELLOW = "yellow",
  GREEN = "green"
}