{
  "name": "@chess4/engine",
  "version": "2.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest --config jest.config.js --runInBand",
    "docs": "typedoc"
  },
  "devDependencies": {
    "typedoc": "^0.28.20"
  }
}
